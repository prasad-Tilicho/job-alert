name: Delete Instagram posts

# KNOWN LIMITATION (verified 2026-09-07): the Instagram API with Instagram Login
# does not support deleting media. Every id returns "Unsupported delete request".
# This workflow is kept because the code is correct and would work if the app is
# ever migrated to the Facebook Login path, which does expose DELETE. Until then,
# remove posts in the Instagram app.
#
# Manual only, and deliberately so: deleting a post cannot be undone.
on:
  workflow_dispatch:
    inputs:
      media_ids:
        description: "Media ids to delete (comma or space separated)"
        required: true
        type: string
      confirm:
        description: "Type DELETE to confirm"
        required: true
        type: string

permissions:
  contents: read

jobs:
  delete:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -e .
      - name: Delete
        env:
          IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}
          MEDIA_IDS: ${{ inputs.media_ids }}
          CONFIRM: ${{ inputs.confirm }}
        run: python scripts/delete_media.py
